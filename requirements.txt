# Required dependencies for Hledac Universal
aiosqlite>=0.19.0
aiohttp>=3.9.0
aiohttp-socks>=0.8.0
# MOVED_TO_OPTIONAL_BROWSER_BY_CP314_CLEANUP: camoufox[geoip]
nodriver>=0.1.0
pytesseract>=0.3.10
stem>=1.8.0
dnspython>=2.4.0
# DISABLED_BY_CP314_CLEANUP: aiobtcdht>=0.1.0  # likely typo/dead package; investigate aiobtdht
pydantic>=2.0.0
beautifulsoup4>=4.12.0

# F196A pre-review fixes: missing runtime dependencies
pyprobables>=0.7.0,<0.8.0        # url_dedup.py RotatingBloomFilter import
httpx>=0.27.0          # fetching/public_fetcher.py HTTP client
pyzipper>=0.3.6,<0.4.0        # security/vault_manager.py encryption

# P13: Vector search and memory monitoring
lancedb>=0.2.5
psutil>=5.9.0

# F206Z: Public discovery — DuckDuckGo primary search (ddgs v9+ primary, duckduckgo-search v8.x fallback)
duckduckgo-search>=8.0.0

# Sprint F195: Fast JSON serialization (required - used throughout codebase)
orjson>=3.9.0

# torch and torchvision are optional — install via:
#   pip install -r requirements-optional.txt
# or for Apple Silicon MPS:
#   pip install torch torchvision
PyYAML>=6.0,<7.0        # yaml import in utils/execution_optimizer.py
msgspec>=0.21.1,<0.22.0     # Canonical DTO/msgspec.Struct serialization
pyahocorasick>=2.3.1,<2.4.0     # provides import ahocorasick for pattern matcher
xxhash>=3.6.0,<4.0.0     # fast hashing for RSS/feed/dedup paths
lmdb>=2.2.0,<3.0.0     # persistent dedup/store backend
