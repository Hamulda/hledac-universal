# Sprint 300s Deep Analysis & Optimization Strategy
**Datum**: 2026-06-20
**Sprint**: 1781919443 (ransomware threat intelligence leak dark web exposure corporate data breach)
**Platform**: MacBook Air M1 8GB UMA

---

## Executive Summary

| Metrika | Hodnota | Status |
|---------|---------|--------|
| Cyklů | 23/22 | ⚠️ |
| Findings | 0 | 🔴 |
| Duplicates | 0 | 🔴 |
| Runtime | 217s (73%) | ⚠️ |
| Early exit | windup_break (90s windup) | 🔴 |
| DuckDB | 9.6GB, EMPTY | 🔴 |

**Verdikt**: Sprint je nefunkční — všechny acquisition lane selhávají na undefined variable, circuit breaker, a/nebo timeout. Potřebuji 3 opravy P0 před jakýmkoliv dalším testováním.

---

## Fáze 1: Root Cause Analysis (P0 Bugs)

### Bug 1 — `NameError: '_keyword_seed_fallback_triggered'`
**Severity**: CRITICAL — blokuje veškerou public lane
```
ERROR: Public pipeline task failed: name '_keyword_seed_fallback_triggered' is not defined
```
- **Lokace**: `fetching/public_fetcher.py` — pravděpodobně undefined variable reference
- **Dopad**: Každý public fetch okamžitě padá, žádné domain seeds se negenerují
- **Fix**: Najít a definovat `_keyword_seed_fallback_triggered` nebo odstranit referenci

### Bug 2 — Circuit Breaker na crt.sh
**Severity**: HIGH — blokuje CT lane
```
WARNING: CT fallback: circuit_breaker_open:circuit_open_failure_threshold_exceeded, trying certstream fallback
WARNING: CT fallback: crt.sh identity search failed: circuit_breaker_open:circuit_open_failure_threshold_exceeded
```
- **Lokace**: `intelligence/ct_log_client.py`
- **Dopad**: CT lane nikdy neprodukuje findings — circuit breaker se aktivuje při prvním dotazu
- **Root cause**: Pravděpodobně crt.sh API rate limiting nebo TLS fingerprinting
- **Fix**: Reset circuit breaker po X minutách, nebo fallback na alternative CT provider

### Bug 3 — Windup Budget Overconsumption
**Severity**: CRITICAL — 90s windup = 43% aktivního času
```
[WINDUP] final_windup=90.0s (explicit)
time_to_windup_s: 219.92s
active_window_budget_s: 210.0s
windup_lead_observed_s: 0.01s
```
- **Lokace**: `SprintScheduler.windup_lead` vzorec — `30% of duration, clamp [30, 180]`
- **Problém**: Pro 300s sprint: 30% = 90s windup. Ale windup trvá 219s (včetně model load)!
- **Efekt**: Aktivní okno = 210s - 219s = **záporné** → všechny lane okamžitě v timeoutu
- **Fix**: Snížit `windup_lead` ceiling na [30, 60] nebo invalidovat aktivní okno při windup > 50% budget

### Bug 4 — DuckDB Shadow Store Not Initialized
**Severity**: HIGH — canonical write path broken
```
INFO: LMDB KV store initialized at .../shadow_wal.lmdb
(sprintech: DuckDB store: ???)
```
- **Sprint report**: `findings_total: ?` — DuckDB se nikdy neinicializoval
- **Dopad**: Všechny findings jdou pouze do LMDB, ne do DuckDB → žádná persistencia
- **Root cause**: `HLEDAC_DUCKDB_INPROCESS=0` default → DuckDB se oddaluje, ale store se nemountuje

---

## Fáze 2: Storage Analysis

### DuckDB — 9.6GB (!)
```
~/.hledac/ioc_graph.duckdb  — 9.6GB
~/.hledac/ioc_graph.duckdb.wal
```
- **Problém**: Databáze existuje, ale je PRÁZDNÁ (sprinty produkují 0 findings)
- **M1 8GB limit**: 9.6GB na SSD, ale při startu se načítá část do RAM
- **Doporučení**: Okamžitě **VACUUM** po každém sprintu, držet pod 2GB

### LMDB Stores — všechny TINY (128B-4KB)
```
federated_models.lmdb       — 128B (prázdná)
local_graph.lmdb            — 128B (prázdná)
keys.lmdb                    — 128B (prázdná)
bandit.lmdb                  — 128B (prázdná)
lmdb/exposure_cache.lmdb     — 128B (prázdná)
ioc_graph.duckdb             — 9.6GB (plná, ale prázdná dat)
```
- **Problém**: LMDB jsou initialized ale empty → žádné cross-sprint persistence
- **Doporučení**: Ověřit že `upsert_ioc` a `upsert_relation` skutečně zapisují

### Cache
```
~/.hledac/cache/             — 128B
~/.hledac/http_cache/         — 64B (prázdná)
```
- **Conditional cache**: 16MB LMDB na `/Users/vojtechhamada/.cache/hledac/conditional_cache`
- **mlx-embeddings**: NOT INSTALLED — fallback na CPU embedder

---

## Fáze 3: Memory & Hardware State

### Pre-sprint (OK)
```
[BOOT] MLX buffers: cache=727MiB wired=1536MiB configured=True
[BOOT] Pre-sprint checks OK | UMA: 4.45GiB used | swap: 0.48GiB
```
- **Volná RAM**: ~3.55GB (4.45GiB used z 8GB)
- **MLX cache**: 727MiB (z 1536MiB wired limit)
- **Swap**: 0.48GiB — žádný swap pressure

### Post-sprint (DEGRADED)
```
[TEARDOWN] UMA delta: +1.02GiB (model load leak?)
```
- **Problém**: Model load neuvolňuje Metal cache správně → 1GB leak per sprint
- **Kumulativní efekt**: Po 3-4 sprintech → swap city → M1 watchdog

### Metal API Deprecation
```
mx.metal.get_active_memory is deprecated → use mx.get_active_memory
mx.metal.get_peak_memory is deprecated → use mx.get_peak_memory
mx.metal.get_cache_memory is deprecated → use mx.get_cache_memory
```
- **Dopad**: Budoucí verze MLX budou mít tyto funkce removed
- **Fix**: Přejít na nové API co nejdřívíve

---

## Fáze 4: Lane Failure Analysis

| Lane | Status | Root Cause |
|------|--------|------------|
| CT (crt.sh) | 🔴 Circuit breaker | crt.sh rate limited / fingerprint blocked |
| CT (certstream) | 🔴 Fallback failed | circuit breaker propagated |
| Public SERP | 🔴 NameError | `_keyword_seed_fallback_triggered` undefined |
| Feed | 🔴 0 findings | domain seeds never generated |
| Passive DNS | ⚠️ Skipped | no domain seeds |
| DOH | ⚠️ Unknown | probably same root cause |
| Graph RAG | ⚠️ Not enabled | HLEDAC_ENABLE_GRAPH_ANALYSIS=0 |

### Acquisition Prelude (16.7s)
```
[prelude] completed in 16.7s (budget=90s)
```
- **Domain seeds**: 0 generated (public lane broken)
- **N/A lanes**: passive_dns, doh, ct — skipped because no seeds

---

## Fáze 5: Optimization Roadmap (Cutting-Edge M1 8GB)

### P0 — Critical Fixes (ihned)

#### P0-1: Fix `_keyword_seed_fallback_triggered` NameError
```python
# Lokace: fetching/public_fetcher.py
# Nahradit undefined reference za správnou proměnnou nebo definovat
_keyword_seed_fallback_triggered = True  # nebo správná logika
```

#### P0-2: Circuit Breaker Reset Policy
```python
# Lokace: transport/circuit_breaker.py
# Přidat TTL na circuit breaker state — reset po 5 minutách
# Místo permanentího open state
```

#### P0-3: Windup Budget Guard
```python
# Lokace: core/__main__.py pre-flight
# Pokud windup_lead > 50% active_window → abort s [F221-ABORT-WINDUP]
# Nebo snížit windup_lead ceiling na 60s maximum
```

### P1 — Performance (tento týden)

#### P1-1: DuckDB In-Process Mode
```bash
HLEDAC_DUCKDB_INPROCESS=1  # -200MB RAM, ale 10× rychlejší writes
```
- **Current**: DuckDB external process → 16ms round-trip per write
- **Target**: DuckDB in-process → 0.5ms per write
- **RAM cost**: +200MB (still OK na 8GB)

#### P1-2: MLX Pre-warm Cache
```python
# Před windup: načíst Hermes3 do Metal cache
# 727MiB už alokováno → udržet resident, ne clear
```
- **Current**: Model load per sprint = 3-5s × 4 = 12-20s lost
- **Target**: Persistent model = 0s load
- **Risk**: Memory leak při accumulation — řešeno v P1-4

#### P1-3: Arrow Zero-Copy Ingest
```bash
HLEDAC_ARROW_INGEST=1  # už zapnuto ✓
```
- **Ověřit**: že Arrow path se skutečně používá (ne fallback na row-by-row)

#### P1-4: Metal Memory Leak Fix
```python
# KRITICKÝ: mx.eval([]) před clear_cache()
# + přidat mx.metal.set_cache_limit(0) PO swap, ne permanentně
# + GC.collect() po každém sprint winddown
```
- **Current leak**: +1.02GiB per sprint
- **Target**: ±0GiB per sprint

### P2 — Advanced (tento měsíc)

#### P2-1: Continuous Batching pro Hermes3
```python
# brain/mlx_batched_executor.py — již existuje (Sprint P0-2)
# Wire: Hermes3Engine → MLXBatchedExecutor.submit()
# Benefit: 2-4× inference throughput na M1
```
- **Ověřit**: že batcher má callerky (Sprint P0-2/P0-3 integration OK)
- **Pravděpodobně již funkční** — `ml_jobs=1` v logu naznačuje možná nenastavený batch_size

#### P2-2: Speculative Decoding
```python
# Hermes3 3B jako draft → 1B jako target (až bude dostupný)
# Nebo: ModernBERT embedder jako draft pro Hermes3
```
- **M1 risk**: Vysoký — vyžaduje 2 modely v RAM současně
- **Fallback**: Použít pouze v EMERGENCY mode (už je)

#### P2-3: Rust LMDB Bulk Counters
```python
# IntCounterLayoutRust — již existuje (Sprint P1-5)
# Wire: hot_edges_cache.py používá batch_bump
# Benefit: 50-80MB → 15-25MB RAM pro LMDB pool
```
- **Ověřit**: že `chain_hash_snapshot` je zavolaný ve winddown

#### P2-4: HTTP/3 Opportunistic Upgrade
```bash
HLEDAC_ENABLE_HTTPX_H3=1  # už zapnuto ✓
# Ověřit: curl_cffi prewarm pool funguje
# ~/.cache/hledac/conditional_cache — 16MB LMDB, 304 hit = ~3ms vs ~3s
```
- **Current status**: Alt-Svc speculative probe běží
- **Risk**: Nízký — fail-soft, fallback na HTTP/1.1

### P3 — Experimental (future)

#### P3-1: Rust SIMD Bloom Filter
- **File**: `rust_extensions/src/bloom_filter.rs`
- **Benchmark**: 10× rychlejší než pybloom_live
- **M1 8GB**: 50MB allocated, OK

#### P3-2: ANE (Apple Neural Engine) Embeddings
```python
# Core ML neboli ANE pro ModernBERT embeddings
# M1 ANE = 11 TOPS, 2.5× rychlejší než GPU pro inference
# Aktuálně: Metal backend (MLX), funguje OK
```
- **Risk**: ANE memory bounded na 256MB, ModernBERT potřebuje ~400MB

#### P3-3: Distributed DuckDB (permissive)
```python
# multiple DuckDB instances pro různé namespace
# DuckDB multi-threaded → 4 threads max na M1 8GB
```
- **M1 risk**: Vysoký — více vláken = více RAM
- **Benefit**: 2-4× parallel scan

---

## Fáze 6: Cutting-Edge Methods (2026)

### 6.1 MLX lazy evaluation — už optimální ✓
- Hermes3 používá `mx.eval()` barrier — správně
- KV cache: `kv_bits=4`, `max_kv_size=8192` v `generate()` — správně

### 6.2 Rust SIMD extensions
```bash
# V rust_extensions/ — již existuje
# Klíčové moduly:
# - url_ops.rs: fast URL parsing (simdjson)
# - content_hasher.rs: BLAKE3 + SHA256 dual-hash
# - bloom_filter.rs: SIMD Bloom filter
```

### 6.3 WebAssembly (WASM) Fallback
- **Neimplementováno** — M1 Safari má WASM, ale projekt nepoužívá
- **Použití**: Lightweight parsery (HTML→markdown) v WASM thread

### 6.4 Quantization (4-bit — již aktivní)
```bash
# Hermes-3-Llama-3.2-3B-4bit — již v produkci ✓
# Memory: ~2GB místo ~6GB pro fp16
```

### 6.5 KV Cache Quantization
```python
# Aktuálně: kv_bits=4 v mlx_lm.generate()
# Cíl: kv_bits=2 pro další 50% úsporu
# Risk: Quality degradation pro long context
```

---

## Fáze 7: M1 8GB Hard Limits

| Resource | Limit | Current Peak | Buffer |
|----------|-------|--------------|--------|
| RAM (system) | 8GB | 5.47GB (4.45 + 1.02) | 2.53GB |
| Metal cache | 1.5GiB | 1.02GiB | 0.48GiB |
| SWAP | Variable | 0.48GB | N/A |
| DuckDB | <2GB target | 9.6GB (!) | — |
| LMDB aggregate | <100MB | <10MB | OK |

**Critical**: DuckDB 9.6GB na 8GB stroji — **OKAMŽITĚ VACUUM**

---

## Action Items (Priority Order)

| # | Akce | Doba | Závislost |
|---|------|------|-----------|
| 1 | Opravit `_keyword_seed_fallback_triggered` | 10min | Žádná |
| 2 | Reset circuit breaker TTL | 30min | Žádná |
| 3 | Snížit windup_lead ceiling na 60s | 15min | Žádná |
| 4 | Vacuum DuckDB | 5min | Žádná |
| 5 | Ověřit Arrow ingest path | 20min | 1+2+3 |
| 6 | Fix Metal memory leak (mx.eval+clear) | 30min | 5 |
| 7 | Přepnout na DuckDB in-process | 10min | 4 |
| 8 | Integrovat batch executor do Hermes3 | 45min | P0-2/P0-3 |
| 9 | MLX pre-warm cache | 30min | 6 |
| 10 | Rust SIMD bulk ops activation | 60min | 7 |

---

## Sprint Scorecard

```
╔══════════════════════════════════════════════════════════╗
║  SPRINT 1781919443 — ransomware threat intelligence      ║
║  Profile: aggressive | Duration: 300s | Exit: windup    ║
╠══════════════════════════════════════════════════════════╣
║  FINDINGS      0/0        ████████████ 0%             ║
║  CYCLES       23/22       ████████████ OK              ║
║  WINDUP        90s         ████████████ 43% ⚠️         ║
║  ACTIVE        210s        ████░░░░░░░░ 19%  🔴        ║
║  DUCKDB        9.6GB!      ████████████ FULL  🔴        ║
║  CIRCUIT BRK   ON          ████████████ BLOCKED 🔴      ║
║  METAL LEAK    +1GB/sprint ████████████ LEAK   🔴        ║
╠══════════════════════════════════════════════════════════╣
║  OVERALL       NON-FUNCTIONAL — 5 P0 bugs block output  ║
╚══════════════════════════════════════════════════════════╝
```

---

*Generated: 2026-06-20 | Vojtech Hamada | MacBook Air M1 8GB*
