# Package Migration Report — 2026-05-23

## Executive Summary

**Task:** Audit root-level Python files and fix broken_imports.json

**Key Findings:**
1. **No migration needed** — all 26 root-level `.py` files are either canonical sources (deep_probe, evidence_log, project_types) or active utilities (embedding_pipeline, capabilities). Zero duplicates found.
2. **broken_imports.json is stale** — `python3 -m py_compile ./*.py` passes for ALL root-level files. The broken_imports.json was generated at wrong namespace depth.
3. **5 files candidates for archive** — `ARCHITECTURE_MAP.py` (23KB, 0 callers), `orchestrator_integration.py` (0 callers), `debug_import.py`, `research_context.py` (1 caller), `runtime_authority_manifest.py` (1 caller).

---

## 1. Root-Level Files Audit

### 1.1 Complete Inventory (sorted by size)

| Size | File | Importers | Recommendation |
|------|------|-----------|----------------|
| 137KB | `__main__.py` | 14 | KEEP — canonical CLI entry |
| 114KB | `enhanced_research.py` | 3 | KEEP — active coordinator |
| 77KB | `evidence_log.py` | 11 | KEEP — evidence logging active |
| 60KB | `project_types.py` | 66 | KEEP — critical shared dataclasses |
| 50KB | `deep_probe.py` | 17 | KEEP — active deep probe |
| 37KB | `embedding_pipeline.py` | 25 | KEEP — embedding pipeline active |
| 34KB | `capabilities.py` | 18 | KEEP — capability enumeration |
| 31KB | `tot_integration.py` | 10 | KEEP — tree-of-thought integration |
| 30KB | `run_comprehensive_tests.py` | 15 | KEEP — test runner |
| 30KB | `autonomous_analyzer.py` | 3 | KEEP — research profile |
| 27KB | `orchestrator_integration.py` | 0 | CANDIDATE — 0 callers, verify deprecation |
| 23KB | `ARCHITECTURE_MAP.py` | 0 | CANDIDATE — 0 callers, 0 imports, no main |
| 22KB | `research_context.py` | 1 | CANDIDATE — verify if needed |
| 16KB | `semantic_deduplicator.py` | 8 | KEEP — active semantic dedup |
| 16KB | `tool_exec_log.py` | 8 | KEEP — tool execution logging |
| 16KB | `resource_allocator.py` | 11 | KEEP — resource allocation |
| 12KB | `smoke_runner.py` | 12 | KEEP — smoke test runner |
| 12KB | `captcha_solver.py` | 4 | CANDIDATE — verify usage |
| 8KB | `__init__.py` | 1 | KEEP — package init |
| 6KB | `tool_registry.py` | 6 | KEEP — tool registry |
| 5KB | `runtime_authority_manifest.py` | 1 | CANDIDATE — verify if debug artifact |
| 1KB | `ci_import_check.py` | 2 | KEEP — CI import check utility |
| 0KB | `debug_import.py` | 3 | CANDIDATE — one-off debug utility |

### 1.2 Duplicate Check Results

| File | Claimed duplicate | Actual status |
|------|-------------------|---------------|
| `autonomous_analyzer.py` | Exists in `brain/` | NO — brain/ has Hermes3Engine separate; root is standalone AutoResearchProfile |
| `embedding_pipeline.py` | Exists in `knowledge/` | NO — knowledge/ has only semantic_store.py (LanceDB wrapper); root is full pipeline |
| `evidence_log.py` | Exists in `knowledge/` | NO — no evidence_log.py in knowledge/; root is sole source |
| `deep_probe.py` | Exists in `forensics/` | NO — forensics/ has no deep_probe.py; root is sole source |

**Conclusion:** No true duplicates. All large root-level files are canonical sources.

---

## 2. broken_imports.json Analysis

### 2.1 Verdict: STALE

`python3 -m py_compile ./*.py` → **ALL ROOT FILES COMPILE OK**

The 281-entry broken_imports.json was generated by a tool that resolved imports at wrong namespace depth (`hledac.universal` → `hledac/hledac/universal`). Most "missing" symbols are already exported.

### 2.2 Stale Entries (already resolved)

| Symbol | Count claimed | Actual status |
|--------|---------------|---------------|
| `build_temporal_priority_hints` | 16 | Already exported in `layers/__init__.py` line 180 |
| `ActionResult` | 13 | Already exported in `utils/__init__.py` line 23 |
| `get_uuid7_compat_status` | 6 | Already exported in `utils/__init__.py` lines 128-151 |
| `ZeroAttributionEngine` | 3 | Module `security/zero_attribution_engine.py` EXISTS |

### 2.3 Truly Missing Modules (~15 unique)

| Missing Module | Count | Status |
|----------------|-------|--------|
| `rl/marl_coordinator.py` | 8 | Module doesn't exist — `rl/` has qmix.py, replay_buffer.py, sprint_policy_manager.py only |
| `runtime/memory_watchdog.py` | 10 | Module doesn't exist |
| `transport/transport_context.py` | 6 | Module doesn't exist |
| `hledac.core.mlx_embeddings.*` | 10 | `hledac/core/` doesn't exist — path confusion with `hledac/universal/` |
| `transport.nym_transport` | 2 | Module doesn't exist in transport/ |
| `brain.ane_embedder` | 3 | Module doesn't exist in brain/ |
| `dht.kademlia_node` | 2 | Module doesn't exist in dht/ |

### 2.4 Namespace Confusion

~42 entries reference `hledac.universal.universal.*` — double `universal` path resolution error from the stale broken_imports.json generator.

### 2.5 Fix Plan

For truly missing modules (not stale entries), investigate each:
1. `rl/marl_coordinator.py` — check if this was renamed/removed (sprint memory mentions `marl_coordinator` was deleted in F196A)
2. `runtime/memory_watchdog.py` — check if replaced by `utils/uma_budget.py`
3. `transport/transport_context.py` — check if should be created or is dead
4. `hledac.core.*` imports — these are wrong path, should be `hledac.universal.core.*` or module doesn't exist

---

## 3. Recommended Actions

### P0 — No broken_imports.json fix needed (stale)

### P1 — Archive dead files (verify first)

| File | Callers | Action |
|------|---------|--------|
| `ARCHITECTURE_MAP.py` | 0 | DELETE — 23KB, no imports, no main, appears generated artifact |
| `orchestrator_integration.py` | 0 | Move to `legacy/` — superseded by autonomous_orchestrator.py v6.2 |
| `debug_import.py` | 3 | Evaluate — may be useful debug utility (keep for now) |
| `research_context.py` | 1 | Verify if superseded by `brain/` context before archiving |
| `runtime_authority_manifest.py` | 1 | Verify if runtime dependency before archiving |

### P2 — Investigate truly missing modules

| Missing Module | Count | Action |
|----------------|-------|--------|
| `rl/marl_coordinator.py` | 8 | Check sprint F196A — may have been deleted as dead code |
| `runtime/memory_watchdog.py` | 10 | Verify if should exist or replaced by uma_budget |
| `transport/transport_context.py` | 6 | Verify if should exist or is dead |
| `hledac.core.*` path confusion | 10 | Fix wrong import paths in source files |

### P3 — No migration needed

---

## 4. What Was NOT Done (And Why)

| Proposed Action | Rejection Reason |
|-----------------|------------------|
| Migrate `deep_probe.py` → `forensics/deep_probe.py` | No existing canonical location; 17 callers would break |
| Migrate `evidence_log.py` → `knowledge/evidence_log.py` | No existing file there; 11 callers would break |
| Migrate `embedding_pipeline.py` → `embeddings/` | `embeddings/` has only modernbert_embedder.py; different scope |
| Migrate `autonomous_analyzer.py` → `analysis/` | `analysis/` doesn't exist; 3 callers would break |
| Fix pyproject.toml `[tool.pyright]` | Existing `pythonVersion = "3.13"` close to 3.14; adding section with different version may cause confusion |

---

## 5. Verification

```bash
# All root-level .py files compile
python3 -m py_compile ./*.py  # → ALL OK

# Check specific imports
python3 -c "from hledac.universal.utils import ActionResult"  # → OK
python3 -c "from hledac.universal.layers import build_temporal_priority_hints"  # → OK

# Run test suite to verify no regressions
pytest tests/ -v --tb=short 2>&1 | tail -20
```

---

## 6. Summary

| Metric | Value |
|--------|-------|
| Root-level .py files | 26 (keep all canonical sources) |
| broken_imports.json entries | 281 (mostly stale, ~15 truly missing) |
| True duplicates found | 0 |
| Files migrated | 0 (none needed) |
| Dead files to archive | 1-5 (ARCHITECTURE_MAP.py + candidates) |
| Compile status | ALL ROOT FILES COMPILE OK |
| pyproject.toml changes | None needed |