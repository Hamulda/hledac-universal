- Exit codes are deterministic: 0=success, 1=runtime error, 2=config/validation (F221-ABORT windup guard), 3=programmer error (NameError/AttributeError/ImportError), 130=SIGINT
- Tests run as subprocesses because pytest intercepts SystemExit — this allows capturing actual sys.exit() codes
- 6 subprocess-based tests in tests/test_exit_codes.py covering all exit code paths
- Windup guard (F221-ABORT) triggers at 30% of duration, clamped to [30, 180] seconds
- Requires _MAIN_FATAL prefix in logs for programmer error tests
- PYTHONPATH must include REPO_ROOT and HLEDAC_PARENT; HLEDAC_ACQUISITION_PROFILE must be "default" for windup guard tests
- Structured catch-all envelope added to __main__.py and core/__main__.py for consistent exit handling